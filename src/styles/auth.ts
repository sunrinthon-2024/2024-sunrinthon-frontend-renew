import { StyleSheet } from 'react-native';

const auth = StyleSheet.create({
  bottomSection: {
    width: '100%',
    position: 'absolute',
    paddingBottom: 20,
    bottom: 0,
  },
});

export default auth;
